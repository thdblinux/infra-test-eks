apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-staging
  namespace: nginx
spec:
  acme:
    server: https://acme-staging-v02.api.letsencrypt.org/directory
    email: thlinux@devopsmt.com.br
    privateKeySecretRef:
      name: letsencrypt-staging
    solvers:
    - http01:
        ingress:
          class: nginx