apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Postegres-app.fullname" . }}-test-connection"
  labels:
    {{- include "Postegres-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Postegres-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
