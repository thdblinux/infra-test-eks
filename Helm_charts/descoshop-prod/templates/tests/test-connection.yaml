apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "descoshop-prod.fullname" . }}-test-connection"
  labels:
    {{- include "descoshop-prod.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "descoshop-prod.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
