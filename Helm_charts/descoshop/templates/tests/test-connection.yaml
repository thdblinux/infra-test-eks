apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "descoshop.fullname" . }}-test-connection"
  labels:
    {{- include "descoshop.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "descoshop.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
